"""Nucleus storage service (serve-loop MVP v0.0).

Durable layer for the walking skeleton: the `/sessions` turn store (C4) and the
trivial model directory (C6). No `/context`, no `/raw` yet — those are later slices.
"""
