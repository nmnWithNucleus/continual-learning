# data-processing service — learn-loop capture skeleton (M0). Python 3.11+.
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
pydantic>=2.6
jsonschema>=4.21
referencing>=0.34     # registry-based schema loading (matches storage); C1/C2 have no $ref
pytest>=8.0

# The `faster_whisper` ASR_BACKEND is LAZY-IMPORTED only when selected — the mock
# default (and every mock unit test) needs none of the below. Install on the ASR
# box to run real speech-to-text:
# faster-whisper>=1.0
