# data-processing service — learn-loop capture skeleton (M0). Python 3.11+.
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
pydantic>=2.6
jsonschema>=4.21
referencing>=0.34     # registry-based schema loading (matches storage); C1/C2 have no $ref
pytest>=8.0

# Real ASR — a STANDING backend since capture M1 (flip ASR_BACKEND=faster_whisper).
# No torch: the stack is ctranslate2/onnxruntime/av/tokenizers. LAZY-IMPORTED only
# when selected, so the mock default (and every mock unit test) never loads it.
faster-whisper>=1.0
