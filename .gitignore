# One-off installers (e.g. cloudflared.deb — the binary is installed system-wide; not source)
*.deb

# Agent session state
.claude/scheduled_tasks.lock

# Local session scratch — kept on disk, never tracked
start.md
